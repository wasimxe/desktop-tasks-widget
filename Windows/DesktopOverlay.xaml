<Window x:Class="DesktopTasks.Windows.DesktopOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Tasks"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False"
        Left="100" Top="100"
        Width="380" Height="550"
        MinWidth="320" MinHeight="400"
        MaxWidth="800" MaxHeight="1000">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                     ResizeBorderThickness="5"
                     CornerRadius="0"
                     GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Value Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <!-- Task Item Template -->
        <DataTemplate x:Key="TaskItemTemplate">
            <Border Margin="0,0,0,8" Background="White" CornerRadius="8" Padding="12"
                    BorderThickness="0,0,0,3" Cursor="Hand" MouseLeftButtonUp="TaskItem_Click"
                    Tag="{Binding}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Priority}" Value="2">
                                <Setter Property="BorderBrush" Value="#E74C3C"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Priority}" Value="1">
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Priority}" Value="0">
                                <Setter Property="BorderBrush" Value="#2ECC71"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1" Color="#000000"/>
                </Border.Effect>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Checkbox -->
                    <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0"
                             IsChecked="{Binding IsCompleted}" Click="TaskCheckBox_Click"
                             Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=InteractiveElementsVisibility}"/>

                    <!-- Task Content -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Medium"
                                  TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#2C3E50"/>
                                    <Setter Property="TextDecorations" Value="None"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                            <Setter Property="Foreground" Value="#95A5A6"/>
                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock x:Name="DescriptionText" Text="{Binding Description}" FontSize="11" Foreground="#7F8C8D"
                                  TextWrapping="Wrap" Margin="0,4,0,0"/>

                        <TextBlock x:Name="DueDateText" FontSize="10" Foreground="#95A5A6" Margin="0,4,0,0">
                            <Run Text="Due: "/>
                            <Run Text="{Binding DueDate, StringFormat='MMM dd, yyyy'}"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Delete Button -->
                    <Button Grid.Column="2" Content="×" FontSize="20" FontWeight="Bold"
                           Width="28" Height="28" VerticalAlignment="Top"
                           Background="Transparent" Foreground="#95A5A6"
                           BorderThickness="0" Cursor="Hand"
                           Click="DeleteTask_Click" Tag="{Binding}"
                           Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=InteractiveElementsVisibility}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="14">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border x:Name="MainBorder" CornerRadius="12" BorderThickness="1" BorderBrush="#E0E0E0">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#F8F9FA" Offset="0"/>
                <GradientStop Color="#FFFFFF" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.15" Color="#000000"/>
        </Border.Effect>

        <Grid x:Name="TaskViewPanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border x:Name="HeaderBorder" Grid.Row="0" Background="#667EEA" CornerRadius="12,12,0,0" Padding="16,12"
                   MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="My Tasks" FontSize="18" FontWeight="SemiBold"
                                  Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock x:Name="LockStatusText" Text="🔒" FontSize="14"
                                  Foreground="White" Opacity="0.9" Margin="8,0,0,0"
                                  VerticalAlignment="Center" ToolTip="Locked"/>
                    </StackPanel>
                    <TextBlock x:Name="TaskCountText" Text="0 tasks" FontSize="11"
                              Foreground="White" Opacity="0.8"
                              HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Task List -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden"
                         Padding="16,12,16,8">
                <ItemsControl x:Name="TaskList" ItemTemplate="{StaticResource TaskItemTemplate}"/>
            </ScrollViewer>

            <!-- Add Task Button -->
            <Border x:Name="AddTaskPanel" Grid.Row="2" Background="#F8F9FA" BorderBrush="#E0E0E0"
                   BorderThickness="0,1,0,0" CornerRadius="0,0,12,12">
                <Button Content="+ Add New Task" FontSize="14" FontWeight="Medium"
                       Background="#667EEA" Foreground="White"
                       BorderThickness="0" Padding="16,12" Margin="16,12"
                       Cursor="Hand" Click="AddTask_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                               CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#5568D3"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>
